import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    input: {
        borderColor: '#000000',
        borderWidth: 1,
        borderRadius: 3,
        marginBottom: 7.5,
        marginTop: 7.5
    },
    container: {
        padding: 15
    }
})

export default styles
